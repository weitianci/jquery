// 1、$("p").css({ "color": "#ff0011"})
    // 设置p元素的css属性名和属性值

// 2、offset
    // 获取元素在当前视口的相对偏移
    // $("p").offset()
    // 设置元素在当前视口的相对偏移
    // $("p").offset({ top: 10, left: 30 })
    // 返回的对象包含两个整型属性：top 和 left，以像素计。此方法只对可见元素有效

// 3、position
    // 获取元素相对父元素的偏移
    // $("p").position()
    // 设置元素相对父元素偏移
    // $("p").position({ top: 10, left: 30 })